// Euclid's algorithm, the oldest algorithm still in daily use.
fn gcd(a: i64, b: i64) -> i64 {
    if b == 0 {
        return a;
    }
    return gcd(b, a % b);
}

fn main(a: i64, b: i64) {
    print("gcd(" + string(a) + ", " + string(b) + ") = " + string(gcd(a, b)));
}
